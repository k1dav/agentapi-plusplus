name: Alert sync issues

permissions:
  contents: read
on:
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Placeholder for custom alert sync logic
        run: echo "TODO: implement custom alert sync logic (phenoShared reusable workflow removed)"
