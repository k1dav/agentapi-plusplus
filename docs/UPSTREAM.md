# Upstream fork policy

Fork of [coder/agentapi](https://github.com/coder/agentapi).

## Sync lanes

1. Merge `sync/upstream-*` branches (upstream wins on API conflicts)
2. Merge `complete-sync` / local feature branches (local wins on superset)
3. Tag backup snapshots before branch prune

## Wave H2 merges

- `sync/upstream-v0.12.2` — merged
- `complete-sync` — merged
